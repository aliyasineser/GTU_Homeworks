
/**
 * This is an exception class for when operand2 = 0 where (operand1 / operand 2)
 * must calculated. There is an exception for this problem already, but still
 * the developer thinks he has to write one.
 *
 * @author aliyasineser
 */
public class DivideByZeroException extends Exception {

    /**
     * Consturctor of the class
     */
    public DivideByZeroException() {
    }
}
